export enum DatasetEnum {
  Image = 'IMAGE',
  Unknown = 'UNKNOWN',
  Video = 'VIDEO',
}

export enum RoleEnum {
  DatasetAdmin = 'dataset-admin',
  DatasetViewer = 'dataset-viewer',
}

export enum AppEnum {
  DataVision = '3',
}
